name: nikto
description: Web server vulnerability scanner
enabled: false

command:
  binary: nikto
  args:
    - "-h"
    - "{{target}}"
    - "-o"
    - "{{output_dir}}/nikto/{{target}}.txt"

depends_on: []

outputs:
  - pattern: "{{output_dir}}/nikto/*.txt"

timeout: 3600

env: {}
