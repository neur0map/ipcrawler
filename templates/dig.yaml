name: dig
description: Comprehensive DNS reconnaissance - zone transfers, subdomains, service discovery, wildcards, internal leakage
enabled: true
pre_scan: true

command:
  binary: bash
  args:
    - "templates/dig-advanced.sh"
    - "{{target}}"

depends_on: []

outputs:
  - pattern: "{{output_dir}}/dig_results.txt"

timeout: 60

env: {}

requires_sudo: false
