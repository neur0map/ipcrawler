name: nmap-simple
description: Simple nmap TCP connect scan (no sudo required)
enabled: true

command:
  binary: nmap
  args:
    - "-sT"
    - "-p"
    - "22,80,443,8080"
    - "-Pn"
    - "{{target}}"

depends_on: []

outputs:
  - pattern: "{{output_dir}}/*.txt"

timeout: 60

env: {}
