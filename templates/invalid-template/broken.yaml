metadata:
  name: "Broken Plugin"
  # Missing required description field
  type: "portscan"
  priority: 1

execution:
  commands:
    - name: "test"
      # Missing required command field
      timeout: 30