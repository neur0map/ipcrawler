{
  "name": "curl-vhost-preset",
  "tool": "curl",
  "preset": "curl.vhost_check",
  "args": ["-H", "Host: {{vhost}}", "http://{{target}}/"],
  "variables": {
    "vhost": "admin.{{target}}"
  },
  "description": "Virtual host discovery using curl preset with custom variables",
  "tags": ["htb", "curl", "vhost", "preset"]
}